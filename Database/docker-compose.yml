services:
  db:
    image: postgres:15-alpine
    container_name: natk-schedule-db
    environment:
      POSTGRES_DB: natk-shedule
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Passw0rd
    ports:
      - "5432:5432"
    volumes:
      - ./natk-schedule.sql:/docker-entrypoint-initdb.d/natk-schedule.sql
